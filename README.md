# 💰 Personal Finance Tracker API

Бэкенд-приложение для учета личных финансов. Позволяет пользователям регистрироваться, управлять своими доходами/расходами и видеть актуальный баланс в реальном времени.

## 🛠 Технологический стек
*   **Язык:** Python 3.12
*   **Фреймворк:** FastAPI (Асинхронность, Pydantic v2)
*   **База данных:** PostgreSQL 15
*   **ORM:** SQLAlchemy 2.0
*   **Безопасность:** JWT (OAuth2), Bcrypt для хэширования паролей
*   **Контейнеризация:** Docker & Docker Compose

## 🚀 Быстрый запуск

Убедитесь, что у вас установлен Docker и Docker Compose.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com
    cd finance-tracker-api
    ```

2.  **Запустите проект одной командой:**
    ```bash
    docker-compose up --build
    ```

3.  **Готово!** Приложение доступно по адресу: `http://localhost:8000`

## 📖 Документация API
После запуска интерактивная документация (Swagger UI) доступна здесь:
👉 [http://localhost:8000/docs](http://localhost:8000/docs)

Там можно протестировать все эндпоинты: регистрацию, логин (кнопка **Authorize**) и CRUD транзакций.

## 🏗 Архитектура проекта
Приложение спроектировано с разделением ответственности (Separation of Concerns):
*   `main.py` — Точка входа и маршрутизация (Routes).
*   `models.py` — Описание таблиц базы данных (SQLAlchemy).
*   `schemas.py` — Валидация и сериализация данных (Pydantic).
*   `crud.py` — Логика взаимодействия с базой данных.
*   `auth.py` — Логика аутентификации и работа с JWT.

## 🛡 Безопасность
*   Пароли хранятся в БД в виде защищенных хэшей.
*   Доступ к транзакциям защищен JWT-токеном.
*   Пользователь может управлять (видеть/удалять) только своими данными (Row Level Privacy).